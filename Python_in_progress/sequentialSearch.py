# coding: utf-8
def sequentialSearch(alist, item):
    pos = 0 # position in the array 
    found = False # default value is false 
    #
    while pos < len(alist) and not found:
        if alist[post] == item:
            found = True
        else:
            pos = pos + 1
    return found

            
